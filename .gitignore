.DS_Store
.venv/
.pytest_cache/
__pycache__/
**/__pycache__/
*.py[cod]
# Rebuildable LaTeX/PDF outputs; docs/paper.pdf is the tracked shareable copy.
/output/

# Local training artifacts
ft_msbert/
ft_msbert_span/
ft_berel/
ft_berel_span/
ft_msbert_span_*/
ft_tavbert_span_*/
*.log

# Local source data
dss_chunks.csv

# Node dependencies
node_modules/

# Local model checkpoints
models/*
!models/README.md
ft_*

# Large local artifacts (unification work)
comparison/cache/
scratch/
experiments/external_finetune/merged_results/
experiments/results/runs/**/predictions.jsonl
experiments/results/runs/**/*.xlsx
experiments/results/runs/**/*.csv

# Large local artifacts: model checkpoints and exploratory runs.
# Kept on disk, never committed — they hold the raw outputs behind the reports
# under comparison/reports/, so do not delete them.
models/
scratch/
